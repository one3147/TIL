  .	: 현재 디렉토리

  ..	: 상위 디렉토리

  ~	: 홈 디렉토리

  *	: 모든 것
	  모든 종류와 길이의 문자를 대체하는 기호

  >	: 기호 왼쪽 명령이 동작하여 출력되어야 하는 내용을 기호 오른쪽에 저장
	  대상이 없으면 생성, 대상이 존재하는 경우에는 기호 개수에 따라 동작이 다르다

	   >	: 덮어쓰기(overwrite)
	   >> 	: 내용 추가(append)

  |	: 파이프(pipe), shift + \
	  CLI의 단점인 '한 줄에 명령어 하나'를 보완하는 기호
	  하나의 대상에 여러 명령어를 적용

	   A | B		: B ( A )

	   ls -l /etc/ | more	: more ( ls -l /etc/ )
